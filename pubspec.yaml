name: redsalud_video
description: Testing videocall

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.1
  socket_io_client: ^0.9.7+1
  flutter_webrtc: ^0.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
